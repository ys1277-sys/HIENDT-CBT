# 이 파일을 .env 로 복사해서 값을 채운다. .env 는 저장소에 올리지 않는다.
#
# 주의 — VITE_ 로 시작하는 값은 빌드할 때 자바스크립트 파일 안에 그대로 박힌다.
# 배포된 사이트에서 개발자도구로 열어보면 누구나 읽을 수 있다.
# 여기에 진짜로 숨겨야 하는 비밀은 넣지 말 것.
# 관리자 비밀번호는 "아무나 들어오지 못하게" 하는 정도의 잠금장치일 뿐이다.

# 실제 값은 여기 말고 .env 에 넣는다. 이 파일은 저장소에 그대로 올라간다.
VITE_ADMIN_PASSWORD=
